{
  "name": "steam-anti-spambot",
  "version": "1.0.0",
  "description": "A simple Steam chatroom bot which listens for certain phrases in messages, deletes the message then bans the user.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Almatrass",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "steam-totp": "^2.1.1",
    "steam-user": "^4.16.2"
  }
}
